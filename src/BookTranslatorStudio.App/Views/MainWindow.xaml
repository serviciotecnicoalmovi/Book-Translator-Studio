<Window x:Class="BookTranslatorStudio.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:viewModels="clr-namespace:BookTranslatorStudio.ViewModels"
        Title="Book Translator Studio"
        Width="1100"
        Height="720"
        MinWidth="900"
        MinHeight="600"
	WindowState="Maximized"
        WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <viewModels:MainWindowViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="72"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="38"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,0,0,1">
            <Grid Margin="28,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel VerticalAlignment="Center">
                    <TextBlock FontSize="22"
                               FontWeight="SemiBold"
                               Text="Book Translator Studio"/>
                    <TextBlock Margin="0,3,0,0"
                               Foreground="{StaticResource MutedTextBrush}"
                               Text="Traducción profesional de libros PDF"/>
                </StackPanel>

                <Border Grid.Column="1"
                        VerticalAlignment="Center"
                        Background="#EEF4FF"
                        CornerRadius="12"
                        Padding="12,5">
                    <TextBlock Foreground="{StaticResource PrimaryBrush}"
                               FontWeight="SemiBold"
                               Text="v0.1.0"/>
                </Border>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="28">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Background="{StaticResource SurfaceBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="10"
                    Padding="24">
                <StackPanel>
                    <TextBlock FontSize="17"
                               FontWeight="SemiBold"
                               Text="Documento de origen"/>
                    <TextBlock Margin="0,7,0,22"
                               Foreground="{StaticResource MutedTextBrush}"
                               TextWrapping="Wrap"
                               Text="Carga el libro que deseas traducir. En esta etapa se validará el archivo y su información básica."/>

                    <Button Command="{Binding SelectPdfCommand}"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Content="Seleccionar PDF"/>

                    <Button Margin="0,10,0,0"
                            Padding="18,9"
                            Command="{Binding ClearSelectionCommand}"
                            Content="Quitar selección"/>

                    <Separator Margin="0,24"/>

                    <TextBlock FontWeight="SemiBold"
                               Text="Estado"/>
                    <TextBlock Margin="0,7,0,0"
                               Foreground="{StaticResource MutedTextBrush}"
                               TextWrapping="Wrap"
                               Text="{Binding StatusMessage}"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2"
                    Background="{StaticResource SurfaceBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="10"
                    Padding="28">
                <Grid>
                    <Grid x:Name="EmptyState">
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsDocumentLoaded}"
                                                 Value="True">
                                        <Setter Property="Visibility"
                                                Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>

                        <StackPanel HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    MaxWidth="420">
                            <Border Width="76"
                                    Height="76"
                                    Background="#EEF4FF"
                                    CornerRadius="38">
                                <TextBlock HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           FontSize="29"
                                           Foreground="{StaticResource PrimaryBrush}"
                                           Text="PDF"/>
                            </Border>
                            <TextBlock Margin="0,22,0,0"
                                       HorizontalAlignment="Center"
                                       FontSize="21"
                                       FontWeight="SemiBold"
                                       Text="Ningún libro seleccionado"/>
                            <TextBlock Margin="0,9,0,0"
                                       TextAlignment="Center"
                                       TextWrapping="Wrap"
                                       Foreground="{StaticResource MutedTextBrush}"
                                       Text="Selecciona un documento PDF para mostrar su nombre, ubicación, tamaño y cantidad de páginas detectadas."/>
                        </StackPanel>
                    </Grid>

                    <Grid x:Name="DocumentState">
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsDocumentLoaded}"
                                                 Value="True">
                                        <Setter Property="Visibility"
                                                Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>

                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="20"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <StackPanel>
                            <TextBlock Foreground="{StaticResource PrimaryBrush}"
                                       FontWeight="SemiBold"
                                       Text="DOCUMENTO CARGADO"/>
                            <TextBlock Margin="0,8,0,0"
                                       FontSize="24"
                                       FontWeight="SemiBold"
                                       TextWrapping="Wrap"
                                       Text="{Binding SelectedDocument.FileName}"/>
                        </StackPanel>

                        <Border Grid.Row="2"
                                Background="#F9FAFB"
                                CornerRadius="8"
                                Padding="22">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="18"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="18"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="18"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock FontWeight="SemiBold" Text="Nombre"/>
                                <TextBlock Grid.Column="1"
                                           TextWrapping="Wrap"
                                           Text="{Binding SelectedDocument.FileName}"/>

                                <TextBlock Grid.Row="2"
                                           FontWeight="SemiBold"
                                           Text="Ubicación"/>
                                <TextBlock Grid.Row="2"
                                           Grid.Column="1"
                                           TextWrapping="Wrap"
                                           Text="{Binding SelectedDocument.FullPath}"/>

                                <TextBlock Grid.Row="4"
                                           FontWeight="SemiBold"
                                           Text="Tamaño"/>
                                <TextBlock Grid.Row="4"
                                           Grid.Column="1"
                                           Text="{Binding SelectedDocument.FormattedSize}"/>

                                <TextBlock Grid.Row="6"
                                           FontWeight="SemiBold"
                                           Text="Páginas detectadas"/>
                                <TextBlock Grid.Row="6"
                                           Grid.Column="1"
                                           Text="{Binding SelectedDocument.DetectedPageCount}"/>
                            </Grid>
                        </Border>
                    </Grid>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="2"
                Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,1,0,0">
            <TextBlock Margin="28,0"
                       VerticalAlignment="Center"
                       Foreground="{StaticResource MutedTextBrush}"
                       Text="Book Translator Studio · .NET 10 · WPF"/>
        </Border>
    </Grid>
</Window>
